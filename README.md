# Beauty AI Frontend

React + TypeScript + Vite frontend prototype for the Beauty AI project.

## Run in Visual Studio / VS Code

```bash
npm install
npm run dev
```

Then open the local Vite URL shown in the terminal.

## Build

```bash
npm run build
```

The project is intentionally self-contained. API/data-access integration can be connected later through the existing backend endpoints.
